export default function Main() {
    return (
        <main>
            <form className="add-ingredient-form">
                <input aria-label="Add ingredient" type="text" placeholder="eg: Tomatoes" />
                <button type="submit">Add ingredient</button>
            </form>
        </main>
    )
}